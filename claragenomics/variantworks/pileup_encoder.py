# Classes and functions to encode pileups

def encode_snp_pileup(variant_pos, bamf_file, window_size = 50, max_reads = 50, channels={"channels"}):
    # Create an N-D pileup of reads covering a variant locus
    pass
